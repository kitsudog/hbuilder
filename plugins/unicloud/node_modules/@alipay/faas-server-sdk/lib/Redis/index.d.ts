import { CloudConfig } from '@alipay/faas-server-utils';
import { Redis as IORedis, RedisOptions as IORedisOptions } from 'ioredis';
export type RedisOptions = IORedisOptions & {
    innerConfig?: CloudConfig;
    url?: string;
};
export declare class Redis extends IORedis {
    #private;
    constructor(options: RedisOptions);
    auth(...args: any[]): Promise<"OK">;
}
