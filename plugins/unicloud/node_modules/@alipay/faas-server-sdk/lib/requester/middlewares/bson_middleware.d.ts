import type { Next } from 'koa';
import type { HttpRequestContext } from '../HttpRequestContext';
export declare function bsonMiddleware(ctx: HttpRequestContext<unknown, unknown>, next: Next): Promise<void>;
