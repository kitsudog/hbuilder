import type { GeoWinthinParam, GeoNearParam, GeoIntersectsParam } from '../../typing';
import { GeoCommand } from '../AbstractCommand';
export declare class GeoWithinCommand extends GeoCommand {
    private readonly val;
    constructor(val: GeoWinthinParam);
    stringify(): object;
}
export declare class GeoNearCommand extends GeoCommand {
    private readonly val;
    constructor(val: GeoNearParam);
    stringify(): object;
}
export declare class GeoIntersectsCommand extends GeoCommand {
    private readonly val;
    constructor(val: GeoIntersectsParam);
    stringify(): object;
}
