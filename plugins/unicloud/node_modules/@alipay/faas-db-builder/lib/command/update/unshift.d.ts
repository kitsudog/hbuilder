import { UpdateCommand } from '../AbstractCommand';
export declare class UnShiftCommand extends UpdateCommand {
    private readonly val;
    constructor(val: any[]);
    get operator(): string;
    stringify(): object;
}
