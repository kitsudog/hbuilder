"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.uts2kotlin = void 0;
const path_1 = __importDefault(require("path"));
const shared_1 = require("@vue/shared");
const shared_2 = require("../../shared");
const uts2kotlin = (options) => {
    (0, shared_1.extend)(options, { clean: true });
    // TODO 开发阶段禁用缓存
    if ((0, shared_1.isFunction)(globalThis.uts2kotlin)) {
        return globalThis.uts2kotlin(options);
    }
    if (!options.tsconfig) {
        options.tsconfig = path_1.default.resolve(__dirname, '../../../lib/tsconfig/tsconfig.json');
    }
    if (!options.typescript) {
        options.typescript = require('../../../lib/typescript');
    }
    if ((0, shared_2.isInHBuilderX)()) {
        options.tsconfigOverride = {
            compilerOptions: {
                typeRoots: [
                    options.inputDir,
                    path_1.default.resolve(process.env.UNI_HBUILDERX_PLUGINS, 'uniapp-cli-vite', 'node_modules'),
                ],
            },
        };
    }
    return require('../../../lib/kotlin').uts2kotlin(options);
};
exports.uts2kotlin = uts2kotlin;
